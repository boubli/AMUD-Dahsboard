## v1.9.2 — Docs currency and release hygiene

### Changed

- **Theme gallery / docs mirror** — Crimson Flare assets synced into the public Theme Gallery; docs theme definitions match the live **41**-theme UI set (removed obsolete v1.8.9 replacements still listed in gallery source)
- **Roadmap** — Recently shipped / Now / Next / Later refreshed for v1.9.x
- **Docs & README surfaces** — version strings, theme counts, localized README status lines, and `llms.txt` brought current
- **Release notes** — backfilled missing `v1.9.1` notes; template aligned with Recommended + Upgrade pattern
- **Contributing** — point at `scripts/ci-check` and note `cargo audit` / SonarCloud config

### Upgrade

```bash
./update-amud.sh
```

Hard-refresh once after update (especially if you use the Theme Gallery on the docs site).

**Recommended:** `v1.9.2`
