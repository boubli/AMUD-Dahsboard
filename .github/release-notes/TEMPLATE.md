## vX.Y.Z — Short title

One-line summary of the release.

### Added
- New features

### Changed
- Behavior or UI changes

### Bugs introduced (previous release)
- Bugs that shipped in the prior version and are addressed here (or still open)

### Bugs fixed in this release
- Specific regressions or defects resolved in this version

### Known issues (still open)
- Unresolved bugs users should know about

### Upgrade

```bash
./update-amud.sh
```

Or pull the latest Docker image: `tradmss/amud-dashboard:latest`

Hard-refresh your browser (Ctrl+Shift+R) after updating if you use the PWA.

**Binaries:** `amud-server`, `amud-agent` (amd64 + arm64), `ui.tar.gz`, and `SHA256SUMS` are attached below.
