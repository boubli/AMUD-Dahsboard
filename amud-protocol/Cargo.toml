[package]
name = "amud-protocol"
version = "1.3.6"
edition = "2021"
description = "Shared wire types for AMUD dashboard ↔ agent IPC"

[dependencies]
serde = { version = "1", features = ["derive"] }
sha2 = "0.11"
hex = "0.4"
