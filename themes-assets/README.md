# themes-assets (legacy)

**Deprecated for runtime.** v1.6.2+ serves all theme assets from `ui/static/themes/` inside `ui.tar.gz`.

This folder may remain as a historical copy of icon SVGs. Regenerate bundled assets with:

```bash
python scripts/generate-theme-overhaul.py
```
